

export type SecretPair = {
    secretName:string,
    secretPlainText:string

}

export type PublicKey = {
    key_id:string,
    key:string
}
export type RepoProfile = {
    owner:string,
    repo:string
}
